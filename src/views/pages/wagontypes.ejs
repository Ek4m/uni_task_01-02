<%- include("../partials/metas")%> <%- include("../partials/header")%>
<main>
  <div class="table_container">
    <h1>List of wagon types</h1>
    <a href="/wagon-types/add"><button>Add wagon type</button></a>
  </div>
  <% if(data.length > 0){%>
  <table>
    <thead>
      <th>Id</th>
      <th>Name</th>
      <th>Created At</th>
      <th>Updated At</th>
      <th>Options</th>
    </thead>
    <tbody>
      <%data.forEach((elem) => {%>
      <tr>
        <td><%=elem.id%></td>
        <td><%=elem.name%></td>
        <td><%=new Date(elem.createdAt)%></td>
        <td><%=new Date(elem.updatedAt)%></td>
        <td><a href="/wagon-types/delete?id=<%=elem.id%>">Delete</a></td>
      </tr>
      <% })%>
    </tbody>
  </table>
  <% }else{%>
  <h1 class="no_elem">No Wagon types</h1>
  <% }%>
</main>
<%- include("../partials/footer")%>
